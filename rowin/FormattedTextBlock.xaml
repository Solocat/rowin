<UserControl x:Class="rowin.FormattedTextBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:rowin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="ThisControl">
    <!--<TextBlock Foreground="White" FontSize="18" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Text="{Binding Text, ElementName=ThisControl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
    </TextBlock>-->

    <TextBlock x:Name="AllText" Foreground="White" FontSize="18" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
        
    </TextBlock>
</UserControl>
